# 浮记 · 把下一步放在眼前

双击 **浮记.exe** 使用。图标已内置，单独复制 EXE 也能显示正确的窗口和托盘图标。

## 记下、开始、完成

- 新用户：**Enter 先记下**，**Ctrl＋Enter 记下并立即开始**。输入框右侧的 ＋ 遵循同一设置，Ctrl＋点击立即开始。
- 已有数据：保留以前的 Enter 立即开始、并行计时习惯。左侧「备份与更多」可切换输入方式和计时模式。
- **单项专注**：开始另一条时自动暂停上一条。切入该模式会先暂停所有记录，选择一件继续。
- **并行计时**：多条、父子可同时运行；任务总时间按至少一条运行的实际时间计算，不叠加重复时段。
- 点记录的 **暂停**：本条及其下所有层级的小步骤一起停止计时，保留已累计用时；其他线路不受影响。继续本条只启动本条，小步骤可按需单独继续，暂停期间不会补算。
- 点记录的 **✓ 完成**：保存用时并收进已完成，同时完成它尚未完成的小步骤。
- 左侧「已完成」可查看、恢复。恢复保持暂停；恢复父项时只重开随它一起完成的小步骤。恢复子项会重开必要的父项，让它可从列表找到。
- 「今日」统计当前任务今天完成的记录数量，包括小步骤；不是今日用时。顶部仍是历史累计用时。

## 留下下一步

- 每条记录中间的「＋ 留下下一步」可写一句回来要接着做的事。
- 已写内容直接显示，点击修改；清空并确定即可删除这句话。
- 暂停不会强制弹出对话框。再次打开时保留下一步，计时仍需手动继续。

## 其他操作

- 双击顶部任务名重命名；拖动顶部移动窗口。
- 每条右边箭头展开其小步骤，再点收起；右侧左上角返回上一层。
- 双击记录标题编辑。右键记录标题、下一步或按钮，选择删除；左侧可撤销上次删除。
- 默认每栏最多显示三条未完成记录；拉高窗口后可显示更多，超出部分滚轮翻阅。
- 顶部 **⌃** 一键折叠成单行任务名与累计计时；点 **⌄** 展开，恢复面板和未提交草稿，计时不受影响。
- **—** 隐藏到托盘，仍计时；双击托盘图标恢复。
- **×** 保存、暂停并退出。收起面板不暂停；切换任务、锁屏、睡眠和退出会暂停。
- 透明度作用于整张卡片与文字，范围 0%～65%。

## 保存与备份

正式数据位于 %LOCALAPPDATA%\FloatingTasks\tasks.json，另有上一版本 .bak。
新版读取旧数据时保留原有任务和用时，新增完成状态、下一步和偏好字段；测试使用独立数据，不操作正式记录。

操作后保存，计时中每两秒保存检查点；暂停且数据不变时不反复写磁盘。突然断电可能损失最近约两秒。下次打开不计离线时间。

左侧「备份与更多」可导出 / 导入 JSON。导入追加、不覆盖；保存失败会提示并重试。
旧版 EXE 可能丢弃新增字段，不建议在同一正式数据上来回运行新旧版本。

## 开发

运行 .\build.ps1 构建正式程序；.\build.ps1 -Test 运行隔离的行为与界面检查。
图标位于 assets/，构建时嵌入 EXE。artifacts/ 存放测试输出及升级前 EXE 副本，可按需清理。

## 直接添加小步骤

每条记录标题右侧新增 ＋ 按钮。点击后直接在该卡片里输入，无需弹窗或先展开右侧面板。
按 Enter 添加并继续输入，Ctrl＋Enter 立即开始计时；普通 Enter 是否计时遵循「备份与更多」中的输入设置。
层级固定为 任务 → 记录 → 小步骤：小步骤不能再拆分，因此右侧面板中的条目不提供 ＋ 和展开按钮。
Esc 取消本次输入。切换记录时，未提交的文字在本次运行中暂存；退出前请提交需要保留的内容。

## AI / MCP 接入

同目录 FloatingTasks.Mcp.exe 是提供给 Agent 的本地 MCP 入口，支持任务、子记录、细分步骤、待办与完成状态。详细操作见 [MCP接入说明.md](MCP接入说明.md)，可合并的配置示例见 mcp.config.example.json。新增待办默认不开始计时。


## 便签
主卡片底部点击“便签”向下展开，可输入多行文字，点击“保存”或 Ctrl+Enter 保存。
选择历史便签可查看全文并编辑；“新建”开始另一条，切换时会先保存非空修改。
支持删除与撤销最近一次删除。折叠面板保留当前草稿，退出前请保存。
便签跨任务共享、不参与计时，和任务一起写入本地备份；每条最多 20000 字符。
大模型可通过 MCP 的 add_note、list_notes、update_note 记录和读取，详见 MCP接入说明.md。

## 调整窗口大小
展开状态下，拖动主面板右边缘、底边缘或右下角斜线即可调整宽高。拉高后可显示更多记录，输入框、便签和步骤面板同步适配。尺寸自动保存，折叠后展开会恢复；右键菜单选择“恢复默认大小”可回到原来的紧凑布局。已运行的旧版需要从菜单“退出并暂停”后重新打开，更新才会生效。
