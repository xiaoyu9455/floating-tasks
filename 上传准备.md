# GitHub 上传准备

建议仓库名称：floating-tasks。

建议描述：Windows 悬浮任务、计时与便签工具，支持拖拽调整大小和本地 MCP 接入。

## 当前目录

本目录是独立的源码上传副本，包含拖拽调整大小的最新实现。没有复制原项目 Git 历史、本地任务数据、备份、个人配置、可执行文件或开发过程截图。

mcp.config.example.json 中的路径是通用示例；实际配置使用本地 mcp.config.json 并保持不提交。

## 使用 Git 上传

1. 在 GitHub 创建一个空仓库，按需要选择公开或私有。不要在创建时生成 README、.gitignore 或许可证文件，以免与本地文件冲突。
2. 在本文件所在目录打开 PowerShell，执行以下命令。将地址中的 YOUR_NAME 替换为你的 GitHub 用户名，并根据实际情况修改仓库名。

~~~powershell
git init -b main
git add .
git status --short
git commit -m "Initial import of Floating Tasks"
git remote add origin https://github.com/YOUR_NAME/floating-tasks.git
git push -u origin main
~~~

如果 Git 提示缺少提交者姓名或邮箱，按你自己的身份配置后再提交。

## 使用网页上传

在空仓库页面选择上传现有文件，将本目录中的文件和子目录上传到仓库根目录，确保 README.md、build.ps1、src/ 和 assets/ 处于同一级。确认 .gitignore 和 .gitattributes 也被上传；不要把外层“GitHub上传准备”目录再包一层。

## 发布前自行确定

- 仓库公开还是私有。
- 许可证类型：本副本没有擅自添加 LICENSE。
- 若发布可直接运行的版本，构建后把两个 EXE 和使用说明打包为 Release 附件，不要提交到源码目录。

验证过程和结果见 [验证记录.md](验证记录.md)。