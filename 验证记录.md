# 上传副本验证记录

验证日期：2026-09-17。

本目录来自独立临时副本的构建和测试，不依赖原项目根目录中的 EXE 或本机 MCP 配置。最终源码与通过测试的副本按 SHA-256 逐文件核对一致。

| 项目 | 结果 |
| --- | --- |
| Windows PowerShell 5.1 正式构建 | 通过，生成桌面程序和 MCP 桥接程序 |
| 行为、存储、界面与协议回归检查 | 79 条 PASS，无失败 |
| MCP stdio 入口检查 | 通过：握手、12 个工具、中文编码、输出及 EOF 退出 |
| 上传文件与已验证副本哈希比对 | 一致 |
| Markdown 本地链接 | 目标文件存在 |
| 本机路径及常见凭据模式扫描 | 未发现匹配 |
| 本地任务、备份、开发工具配置与原 Git 历史 | 未复制 |
| 测试产物、可执行文件 | 未包含在本目录 |

README 的演示截图由隔离界面测试生成，只包含测试数据。

PowerShell 脚本使用 UTF-8 BOM，保证 Windows PowerShell 5.1 能正确读取中文路径和字符串。MCP 配置提供通用路径示例，使用时需修改为自己的安装位置。

本次只准备上传文件，未创建远程仓库、未提交到 GitHub，也未替项目指定许可证。